import { FlutterEngine } from '@ohos/flutter_ohos';

/**
 * ⚠️ 自动生成占位文件 — 请勿手工编辑插件清单。
 *
 * 本文件由 Flutter-OH 工具链（flutter_flutter oh-3.35.7-dev）
 * 在执行 `flutter pub get` / `flutter build hap` 时根据 pubspec.yaml
 * 中声明的插件自动重生成，逐个调用各 ohos 插件的 registerWith。
 *
 * 当前为空实现，仅保证 DevEco Studio 直接打开工程时可编译；
 * 首次用 Flutter-OH 工具链构建后会被真实注册逻辑覆盖。
 */
export class GeneratedPluginRegistrant {
  static registerWith(flutterEngine: FlutterEngine): void {
    // 由 Flutter-OH 构建工具重新生成
  }
}
